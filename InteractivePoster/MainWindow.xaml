<Window x:Class="InteractivePoster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InteractivePoster"
        mc:Ignorable="d"
        Title="Основы аналитической геометрии" Height="450" Width="800" WindowStartupLocation="CenterScreen"  WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <GroupBox Header="Масштаб области">
                <Slider x:Name="Area" SmallChange="2" LargeChange="2" Value="20" Minimum="2" Maximum="50"
                        PreviewMouseMove="Area_PreviewMouseMove" 
                        IsSnapToTickEnabled="True" TickFrequency="2"/>
            </GroupBox>
            <GroupBox Header="Смещение по оси абсцисс">
                <Slider x:Name="slCoordX" SmallChange="1" LargeChange="1" Value="0" Minimum="-50" Maximum="50" PreviewMouseMove="Area_PreviewMouseMove"/>
            </GroupBox>
            <GroupBox Header="Смещение по оси ординат">
                <Slider x:Name="slCoordY" SmallChange="1" LargeChange="1" Value="0" Minimum="-50" Maximum="50" PreviewMouseMove="Area_PreviewMouseMove"/>
            </GroupBox>
            <GroupBox Header="Радиус окружности">
                <Slider x:Name="slRadius" SmallChange="1" LargeChange="1" Value="1" Minimum="0" Maximum="50" PreviewMouseMove="Area_PreviewMouseMove"/>
            </GroupBox>
        </StackPanel>
        <Canvas x:Name="Background" Grid.Column="0" SizeChanged="UpdateBackPattern"  Tag="{Binding ElementName=Area,Path=Value}"/>
        <!--к свойству Tag у канвы биндим значение слайдера с масштабом. Not magic, science-->
    </Grid>
</Window>
